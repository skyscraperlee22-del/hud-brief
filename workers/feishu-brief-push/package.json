{
  "name": "feishu-brief-push",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "post-publish-send": "node scripts/post-publish-send.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
